language: python
sudo: enabled
python:
  - 3.6

matrix:
  include:
    - language: generic
      os: osx
      before_install:
        - brew update
        - brew install python3

before_install:
  - sudo apt-get update -qq

install:
  - pip install ansible

script:
  - make check
  - make play
